export default function ScrollSection() {
  return (
    <section className="pointer-events-auto relative z-10 mx-auto mt-[min(105vh,1200px)] max-w-2xl px-6 pb-40 md:px-12">
      <div className="rounded-2xl border border-white/[0.08] bg-white/[0.02] p-8 backdrop-blur-xl">
        <p className="text-[10px] font-semibold uppercase tracking-[0.35em] text-cyan-400/80">
          Depth layer
        </p>
        <h2 className="mt-3 font-display text-2xl font-bold uppercase tracking-tight text-white md:text-3xl">
          Calm velocity
        </h2>
        <p className="mt-4 text-sm leading-relaxed text-slate-400 md:text-base">
          Lenis keeps vertical motion fluid while the WebGL layer stays fixed. Use the command desk for Mongo-backed
          tasks, the AI robot for study dialogue, and the audio core for focus sound.
        </p>
      </div>
    </section>
  );
}
